.btn-group
	%a.btn.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
		{{= yield}}
		%span.caret
	%ul.dropdown-menu
		{{ _(links).each(function(link){  }}
		%li
			%a{:href => "{{= link.href }}"}{{= link.text }}	
		{{ });}}	